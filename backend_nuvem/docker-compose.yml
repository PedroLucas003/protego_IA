services:
  mosquitto:
    image: eclipse-mosquitto:2
    container_name: protego_mosquitto
    ports:
      - "1883:1883"
    volumes:
      - ./mqtt_broker/config:/mosquitto/config
      - ./mqtt_broker/data:/mosquitto/data
      - ./mqtt_broker/log:/mosquitto/log
    restart: unless-stopped